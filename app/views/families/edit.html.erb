<!-- Validation failure messages -->
<% if @family.errors.any? %>
<% @family.errors.full_messages.each do |message| %>
<div class="alert alert-dismissable alert-danger">
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
  <%= message %>
</div>
<% end %>
<% end %>

<div class="page-header">
  <h3>Edit Family #<%= @family.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_family/<%= @family.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <input type="hidden" id="provider_id" name="provider_id" class="form-control" value="<%= current_provider.id %>">

      <!-- Label and input for family name -->
      <div class="form-group">

        <input type="text" id="name" name="name" placeholder="Family Name" class="form-control" value="<%= @family.name %>">
      </div>

      <button class="btn btn-warning">
        Update Family
      </button>
      or
      <a href="/families/<%= @family.id %>">Cancel</a>
    </form>
  </div>
</div>
